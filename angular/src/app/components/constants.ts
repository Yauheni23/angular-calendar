export enum EventListener {
    MouseDown = 'mousedown',
    KeyDown = 'keydown',
    Click = 'click',
}

export enum KeyBoard {
    Escape = 'Escape',
    Enter = 'Enter',
    Delete = 'Delete',
    ArrowLeft = 'ArrowLeft',
    ArrowRight = 'ArrowRight',
}
