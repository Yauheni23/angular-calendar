export enum eventListener {
    MouseDown = 'mousedown',
    KeyDown = 'keydown',
    Click = 'click',
}

export enum keyBoard {
    Escape = 'Escape',
    ArrowLeft = 'ArrowLeft',
    ArrowRight = 'ArrowRight',
}
